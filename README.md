# Daily Briefing App

A personalized morning briefing tool powered by Claude AI.
Fetches current weather and news headlines, then generates
a clean AI summary to start your day.

## Setup
1. Clone this repo
2. Create a `.env` file with your API keys (see `.env.example`)
3. Run `pip install -r requirements.txt`
4. Run `streamlit run app.py`